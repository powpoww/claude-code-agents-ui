// MIME types used as drag/drop dataTransfer keys.
// Centralized to avoid typos that would silently break drop targets.
export const DND_PROJECT = 'application/x-project'
export const DND_CATEGORY = 'application/x-category'
